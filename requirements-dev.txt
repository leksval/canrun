# Development dependencies for ML model training
# Install with: uv add --dev -r requirements-dev.txt

# Core ML dependencies
scikit-learn>=1.7.0
joblib>=1.5.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Advanced ML libraries for training
xgboost>=2.0.0
# PyTorch for CUDA support (CPU-only version by default)
torch>=2.0.0

# Testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0

# For ML model training and development
# These are NOT required for end users, only for training the ML models
# The production plugin uses pre-trained models from data/ directory