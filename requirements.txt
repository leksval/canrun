# CanRun Universal Game Compatibility Checker v1.1.0
# Production dependencies for G-Assist plugin
# 
# For development setup, use: uv sync --dev
# For production setup, use: uv pip install -r requirements.txt

# System hardware detection (RTX/GTX focus)
psutil>=5.9.0          # System hardware detection (CPU, memory, disk)
GPUtil>=1.4.0          # GPU detection and monitoring
nvidia-ml-py>=12.535.108  # NVIDIA GPU management library for RTX/GTX
py-cpuinfo>=9.0.0      # Detailed CPU information extraction
pynvml>=11.5.0         # Enhanced NVIDIA GPU monitoring
setuptools>=65.0.0     # Compatibility layer for distutils (required for GPUtil)

# Windows-specific dependencies (G-Assist requires Windows)
WMI>=1.5.1;platform_system=="Windows"  # Windows Management Instrumentation
pywin32>=223;platform_system=="Windows"  # Windows API access and COM interface
pyinstaller>=6.11.0;platform_system=="Windows"  # Windows executable packaging

# HTTP requests and async operations
requests>=2.31.0       # HTTP requests for Steam API and external data
aiohttp>=3.8.0         # Async HTTP client for requirements fetching
beautifulsoup4>=4.12.0 # HTML parsing for web scraping

# Data processing and utilities
python-dateutil>=2.8.0  # Date/time handling utilities
typing-extensions>=4.0.0  # Enhanced type hints support

# User experience enhancements
colorama>=0.4.6        # Cross-platform colored terminal output
tqdm>=4.65.0          # Progress bars for long operations

# G-Assist integration dependencies
# Note: torch removed as CUDA detection moved to nvidia-ml-py and pynvml

# Runtime testing (minimal for G-Assist plugin validation)
pytest>=7.0.0         # Testing framework for plugin validation
pytest-asyncio>=0.21.0  # Async testing support

# Note: Development dependencies (black, mypy, etc.) are defined in pyproject.toml
# Install them with: uv sync --dev